class Resources:
    """ Config resources.

    Member:
    resources -- Resource id to resource mapping (dict).
    types -- Type id to type mapping (dict).
    """

    def __init__(self):
        self.resources = {}
        self.types = {}
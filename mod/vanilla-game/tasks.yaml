ai:
    namespace: ai

    tasks:
        namespace: task
        variance_min: -0.2
        variance_max: 0.2
        
        task:
            - name: idle
              type: task.type.idle
              parameters:
                - type: task.parameter.time
                  value: 2
            - name: randomgoto
              type: task.type.randomgoto
#include "configuration/ConfigNode.h"

#include <iostream>

int main()
{
    // Temporary to prevent stupid error.
    for (int i = 0; i < 100; ++i)
    {
        std::cout << i << std::endl;
    }

    return 0;
}

